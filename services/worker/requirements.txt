# ── Web server ─────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# ── Audio download ─────────────────────────────────────────────────────────────
yt-dlp>=2025.1.1

# ── Audio processing ───────────────────────────────────────────────────────────
ffmpeg-python==0.2.0
soundfile==0.12.1

# ── Vocal separation ───────────────────────────────────────────────────────────
# Install torch first: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
demucs==4.0.1

# ── Transcription backends (tried in priority order: whisperx → faster-whisper → openai-whisper)
# ── Option A: WhisperX — best for Japanese word-level timestamps + forced alignment
git+https://github.com/m-bain/whisperX.git
transformers>=4.39.0
pandas>=1.4.0
nltk>=3.8.0
#
# ── Option B: faster-whisper — CTranslate2, word-level via VAD, no alignment model needed
faster-whisper>=1.2.0
#
# ── Option C: openai-whisper — original, segment-level + approximate word timestamps
# openai-whisper==20240930  # removed: pkg_resources build failure; faster-whisper covers this

# ── Forced alignment fallbacks ─────────────────────────────────────────────────
# aeneas: DTW-based, language-configurable, good Japanese support
#   System deps: brew install espeak / apt-get install espeak libespeak-dev
#   pip install aeneas  (separate install due to C build deps)
#
# Gentle: run locally: docker run -p 8765:8765 lowerquality/gentle
#   No pip install needed — the worker calls it via HTTP

# ── Lyrics scraping ────────────────────────────────────────────────────────────
httpx==0.27.2
requests>=2.31.0
beautifulsoup4==4.12.3
lxml==5.3.0

# ── Japanese text normalization ────────────────────────────────────────────────
jaconv==0.3.4

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
